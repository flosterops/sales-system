export enum EButtonsVariants {
  primary = 'primary',
  text = 'text',
  link = 'link',
  turquoise = 'turquoise',
}

export enum EButtonTypes {
  button = 'button',
  submit = 'submit',
  reset = 'reset',
}
