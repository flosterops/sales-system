export const urls = {
  auth: () => '/user/me',
  refresh: (query: string): string => `/oauth/token${query}`,
  login: (query: string): string => `/oauth/token${query}`,
  changePassword: (): string => '/change-password',
  resetPassword: (): string => '/reset-password',
  resetPasswordByToken: (token: string): string => `/reset-password/${token}`,
  userApps: (): string => '/user/apps',
  userSearch: (): string => '/user/search',
  createUser: (): string => '/user/',
  getUser: (id: string): string => `/user/${id}`,
  createGroup: (): string => '/group/',
  getGroup: (id: string) => `/group/${id}`,
  deleteGroupUser: (id: string, userId: string) => `/group/${id}/${userId}`,
  getGroupPermissions: (id: string) => `/group/${id}/permissions`,
  getGroupUsers: (id: string) => `/group/${id}/users`,
  editGroup: (id: string) => `/group/${id}`,
  appPermissions: (id: string): string => `/app/${id}/permissions`,
  groupSearch: (): string => '/group/search',
  appSearch: (): string => '/app/search',
  groupAddUser: (id: string) => `/group/${id}/add-user`,
};
